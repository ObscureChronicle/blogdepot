---
type Props = {
    params?: { label: string; value: string | number }[];
};

const { params } = Astro.props as Props;
---

{
    params && params.length > 0 ? (
        <>
            <h2 class="bg-blue-100 text-blue-800 text-2xl font-bold px-2.5 pt-1.5 rounded-sm dark:bg-blue-200 dark:text-blue-800 inline-block">兵种参数</h2>
            <div class="flex flex-col pl-4 gap-1">
                {params.map((p) => (
                    <p class="text-base leading-relaxed">
                        {p.label}：{p.value}
                    </p>
                ))}
            </div>
        </>
    ) : null
}
